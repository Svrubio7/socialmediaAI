# =============================================================================
# Root .env.example — Shared variables for Docker Compose
# =============================================================================
# Copy to .env and fill in your values.
# Docker Compose reads this file for build args and runtime env vars.
# For local dev (without Docker), each service reads its own .env file instead.

# Supabase (get from Supabase Dashboard → Project Settings → API)
NUXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NUXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-or-publishable-key
NUXT_PUBLIC_SUPABASE_STORAGE_BUCKET=videos
