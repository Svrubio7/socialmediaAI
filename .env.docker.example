# =============================================================================
# Social Media AI - Docker Environment Variables (Optional)
# =============================================================================
# Docker Compose loads frontend/.env and backend/.env automatically.
# Ensure frontend/.env has NUXT_PUBLIC_SUPABASE_URL and NUXT_PUBLIC_SUPABASE_ANON_KEY.
# Ensure backend/.env has SUPABASE_*, GEMINI_API_KEY, OPENAI_API_KEY, etc.
#
# Optional: copy this file to .env.docker and set vars here if you prefer
# a single file; then run: docker compose --env-file .env.docker up -d
# =============================================================================

# -----------------------------------------------------------------------------
# Database (Local PostgreSQL - used by docker-compose)
# -----------------------------------------------------------------------------
# Set automatically in docker-compose.yml

# -----------------------------------------------------------------------------
# Redis (Local Redis - used by docker-compose)
# -----------------------------------------------------------------------------
# Set automatically in docker-compose.yml

# -----------------------------------------------------------------------------
# Supabase
# Backend: SUPABASE_URL, SUPABASE_KEY (service role), SUPABASE_JWT_SECRET
# Frontend: NUXT_PUBLIC_SUPABASE_URL, NUXT_PUBLIC_SUPABASE_ANON_KEY (anon/publishable key)
# Get all from Supabase Dashboard → Project Settings → API
# -----------------------------------------------------------------------------
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-service-role-key
SUPABASE_JWT_SECRET=your-jwt-secret

# Frontend (or set in frontend/.env)
NUXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NUXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-or-publishable-key


# -----------------------------------------------------------------------------
# AI API Keys
# -----------------------------------------------------------------------------
# Google Gemini API - Get from https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key

# OpenAI API - Get from https://platform.openai.com/api-keys
OPENAI_API_KEY=your-openai-api-key

# -----------------------------------------------------------------------------
# Security Keys
# -----------------------------------------------------------------------------
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
ENCRYPTION_KEY=your-32-byte-encryption-key-here
SECRET_KEY=your-secret-key-for-jwt

# -----------------------------------------------------------------------------
# OAuth - Social Media Platform Credentials
# Only needed if you want to test publishing functionality
# -----------------------------------------------------------------------------

# Instagram - https://developers.facebook.com/docs/instagram-api
INSTAGRAM_CLIENT_ID=
INSTAGRAM_CLIENT_SECRET=

# TikTok - https://developers.tiktok.com/doc/marketing-api-overview
TIKTOK_CLIENT_KEY=
TIKTOK_CLIENT_SECRET=

# YouTube - https://developers.google.com/youtube/v3
YOUTUBE_CLIENT_ID=
YOUTUBE_CLIENT_SECRET=

# Facebook - https://developers.facebook.com/docs/graph-api
FACEBOOK_APP_ID=
FACEBOOK_APP_SECRET=

# -----------------------------------------------------------------------------
# Frontend API URL (used during build)
# -----------------------------------------------------------------------------
# For local Docker development, this should point to nginx
NUXT_PUBLIC_API_URL=http://localhost:8000/api/v1