# =============================================================================
# Social Media AI - Docker Environment Variables (Optional)
# =============================================================================
# For dev/Docker only. Production env is set in Render Dashboard.
# DATABASE_URL must be your Supabase Postgres connection string (not a local
# Postgres container). Docker Compose loads frontend/.env and backend/.env by default.
# Optional: copy to .env.docker and run: docker compose --env-file .env.docker up -d
# =============================================================================

# -----------------------------------------------------------------------------
# Database (Supabase only – set in backend/.env or here when using --env-file)
# -----------------------------------------------------------------------------
# DATABASE_URL=postgresql://postgres.[ref]:[password]@...pooler.supabase.com:6543/postgres

# -----------------------------------------------------------------------------
# Redis (set in compose for dev; production = Render Redis)
# -----------------------------------------------------------------------------
# Set in docker-compose.yml for dev (redis://redis:6379)

# -----------------------------------------------------------------------------
# Supabase
# Backend: SUPABASE_URL, SUPABASE_KEY (service role), SUPABASE_JWT_SECRET
# Frontend: NUXT_PUBLIC_SUPABASE_URL, NUXT_PUBLIC_SUPABASE_ANON_KEY (anon/publishable key)
# Get all from Supabase Dashboard → Project Settings → API
# -----------------------------------------------------------------------------
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-service-role-key
SUPABASE_JWT_SECRET=your-jwt-secret

# Frontend (or set in frontend/.env)
NUXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NUXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-or-publishable-key


# -----------------------------------------------------------------------------
# AI API Keys
# -----------------------------------------------------------------------------
# Google Gemini API - Get from https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key

# OpenAI API - Get from https://platform.openai.com/api-keys
OPENAI_API_KEY=your-openai-api-key

# -----------------------------------------------------------------------------
# Security Keys
# -----------------------------------------------------------------------------
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
ENCRYPTION_KEY=your-32-byte-encryption-key-here
SECRET_KEY=your-secret-key-for-jwt

# -----------------------------------------------------------------------------
# OAuth - Social Media Platform Credentials
# Only needed if you want to test publishing functionality
# -----------------------------------------------------------------------------

# Instagram - https://developers.facebook.com/docs/instagram-api
INSTAGRAM_CLIENT_ID=
INSTAGRAM_CLIENT_SECRET=

# TikTok - https://developers.tiktok.com/doc/marketing-api-overview
TIKTOK_CLIENT_KEY=
TIKTOK_CLIENT_SECRET=

# YouTube - https://developers.google.com/youtube/v3
YOUTUBE_CLIENT_ID=
YOUTUBE_CLIENT_SECRET=

# Facebook - https://developers.facebook.com/docs/graph-api
FACEBOOK_APP_ID=
FACEBOOK_APP_SECRET=

# -----------------------------------------------------------------------------
# Frontend API URL (used during build/runtime)
# -----------------------------------------------------------------------------
# Unified container runtime serves API on the same origin at /api/v1.
NUXT_PUBLIC_API_URL=/api/v1
NEXT_PUBLIC_FASTAPI_URL=/api/v1
NUXT_PUBLIC_EDITOR_FORK_ENABLED=false
NUXT_PUBLIC_EDITOR_FORK_ROLLOUT_PERCENT=0
NUXT_PUBLIC_EDITOR_FORK_ALLOWLIST=
EDITOR_ENGINE_DEFAULT=opencut
EDITOR_OPENCUT_ENABLED=true
EDITOR_API_DIAGNOSTICS=false
